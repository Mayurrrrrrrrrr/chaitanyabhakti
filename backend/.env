# Local Database Configuration
DB_HOST=localhost
DB_USER=vaishnav_user
DB_PASSWORD=password123
DB_NAME=vaishnavbhakti

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Secret Key
JWT_SECRET=this-is-a-very-secret-key-for-development

# CORS Configuration (supports localhost, HTTP, and HTTPS)
CORS_ORIGIN=*

# Application URL (used for generating links)
APP_URL=https://haribol.yuktaa.com

# OTP Configuration
OTP_EXPIRY_MINUTES=10

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=52428800
